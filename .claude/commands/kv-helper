---
description: "开启 KVEngine 工业级协作模式"
triggers:
  - kv-helper
---

# kv-helper 命令

用于在这个仓库里开启默认工程协作模式。

适用范围：

- 排查 bug
- 实现功能
- 补回归测试
- 提交前验证
- review 风险点

默认要求：

- 先分析语义，再动手改代码。
- 先补证据，再给结论。
- 先保证正确性，再考虑优化。
- 触碰持久化、并发、协议、RAFT、事务时默认按高风险处理。

调用方式：

- `kv-helper`
